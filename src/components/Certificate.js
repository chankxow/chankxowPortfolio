import React from "react";
import { motion } from "framer-motion";
import { fadeIn } from "../variants";
const Certificate = () => {
  return (
    <div id="certificate" className="section text-black">
    <div className="container"></div>
      Certificate
    </div>
  );
};

export default Certificate;
